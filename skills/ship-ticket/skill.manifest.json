{
  "id": "ship-ticket",
  "name": "Ship Ticket (Shape A pipeline)",
  "description": "The legacy Shape A Jira pipeline reframed as a skill: branch, implement, submit, and ship a referenced ticket via a headless Claude Code session.",
  "triggers": ["ship-ticket"],
  "surfaces": ["deck", "nl"],
  "presentation": {
    "label": "Ship Ticket",
    "icon": "rocket",
    "group": "Workflows",
    "inputs": [
      { "name": "ticketId", "type": "string", "label": "Ticket ID", "description": "e.g. SCA-431", "required": true }
    ]
  },
  "modelPolicy": {
    "execTier": "heavy",
    "privacy": "cloud-ok",
    "pin": "claude-code"
  },
  "execution": {
    "kind": "claude-headless",
    "promptTemplate": "Work the Jira ticket {{ticketId}}: create a branch, implement the change, run tests, and prepare it to ship.",
    "args": ["--model", "claude-opus-4-8"]
  },
  "vaultOutput": "30-tickets/{{ticketId}}.md"
}
