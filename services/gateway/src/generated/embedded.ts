/**
 * AUTO-GENERATED placeholder — overwritten by `npm run embed` (scripts/embed.mjs)
 * before a packaged `bun build --compile`, which bakes the built HUD and the
 * skill manifests into the binary. In dev these stay empty and the gateway reads
 * the HUD (apps/hud/dist) and manifests (skills/) from disk instead.
 *
 * The committed version is intentionally EMPTY; do not commit a generated one.
 */
export const HUD_ASSETS: Record<string, { type: string; b64: string }> = {};
export const SKILL_MANIFESTS: unknown[] = [];
