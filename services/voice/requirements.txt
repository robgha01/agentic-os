# Core service
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
httpx>=0.27

# Local STT (no API key)
faster-whisper>=1.0

# Local TTS (no API key)
kokoro>=0.9
soundfile>=0.12
numpy>=1.26

# Cloud providers are optional and used only when the matching provider is
# selected (keys read from env): openai (Whisper/TTS), elevenlabs (TTS).
# openai>=1.50
# elevenlabs>=1.9
