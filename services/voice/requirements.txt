# Core service
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
httpx>=0.27

# Local STT (no API key)
faster-whisper>=1.0

# Local TTS (no API key)
kokoro>=0.9
soundfile>=0.12
numpy>=1.26

# Optional local TTS alternative — kokoro-onnx runs Kokoro on ONNX Runtime (no
# torch, lighter/faster on CPU). Enable with AGENTIC_OS_TTS_PROVIDER=kokoro-onnx
# and download kokoro-v1.0.onnx + voices-v1.0.bin into services/voice/models/
# (see README). Uses the soundfile/numpy already listed above.
# kokoro-onnx>=0.4

# Cloud providers are optional and used only when the matching provider is
# selected (keys read from env): openai (Whisper/TTS), elevenlabs (TTS).
# openai>=1.50
# elevenlabs>=1.9
